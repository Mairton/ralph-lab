# Ralph Progress Log
Started: sáb 19 set 2026 19:23:29 -03
---
## Codebase Patterns
- Pipeline vive em `pipeline/build.py`; cada etapa e uma funcao `build_*(root: Path)` que le/grava na RAIZ do repo (`ROOT = Path(__file__).resolve().parents[1]`). Artefatos: vendas_lojas.csv, pivot_receita.csv, index.html na raiz.
- Sempre `pd.read_csv(..., dtype={"receita_brl": float})`; o CSV tem valores como `349.5` e `1200`.
- Testes em `tests/test_*.py` usam `ROOT = Path(__file__).resolve().parents[1]`, importam `from pipeline.build import ...` e chamam o `build_*` numa fixture `scope="module"` antes de ler o artefato. Rodar `python -m pytest -q` da raiz (rootdir = raiz, entao `pipeline` e importavel sem conftest).
- Comparacoes de receita: `pytest.approx(valor, abs=0.5)`.
- Typecheck = `python -m py_compile <arquivos .py alterados>`.
- vendas.csv e lojas.csv sao fonte intocavel; confirmar com `git status --porcelain vendas.csv lojas.csv` antes de commitar.
- Nao commitar `ralph-run.log` (log do loop, esta no .gitignore).
---
## 2026-09-19 19:24 - US-001
- Implementado `pipeline/__init__.py` e `pipeline/build.py` com `build_join(root)`: le vendas.csv/lojas.csv (receita_brl como float), `merge(how="inner", on="id_loja")`, grava vendas_lojas.csv na raiz (420 linhas de dados) e imprime resumo no stdout (423 entrada; 3 orfas id_loja=999 R$ 8120.00; loja 108 Batel/PR sem vendas).
- Criado `tests/test_join.py` com 4 testes (420 linhas, receita total 931274.06 +-0.5, colunas preservadas/acrescentadas, ausencia de 999 e 108).
- Adicionado CLAUDE.md na raiz com as convencoes do projeto e `ralph-run.log` ao .gitignore.
- Arquivos: pipeline/__init__.py, pipeline/build.py, tests/test_join.py, vendas_lojas.csv, CLAUDE.md, .gitignore, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Verificacao: `python -m py_compile` OK; `python -m pytest -q` = 4 passed; `git status --porcelain vendas.csv lojas.csv` vazio.
- **Learnings for future iterations:**
  - O join inner com pandas preserva a ordem de vendas.csv e acrescenta nome_loja, regiao, uf, gerente ao final; cabecalho resultante: id_venda,data,id_loja,categoria,unidades,receita_brl,nome_loja,regiao,uf,gerente.
  - `to_csv(index=False)` reescreve `1200.0`/`349.5` como float (ex.: `1200.0`); isso e esperado, a soma bate em 931274.06.
  - US-002 deve ler vendas_lojas.csv (nao vendas.csv) e derivar `mes = data.str[:7]`; regioes em ordem alfabetica saem naturalmente do pivot_table (index ordenado).
  - Sem ferramenta de browser disponivel nesta sessao; para US-003 validar o HTML por parsing nos testes.
---
## 2026-09-19 19:35 - US-002
- Implementado `build_pivot(root)` em `pipeline/build.py`: le vendas_lojas.csv (receita_brl float), deriva `mes = data.str[:7]`, `pivot_table(index="regiao", columns="mes", values="receita_brl", aggfunc="sum", fill_value=0.0)`, ordena linhas/colunas, zera `columns.name` e grava pivot_receita.csv na raiz com `float_format="%.2f"`. Bloco `__main__` agora roda build_join + build_pivot.
- Criado `tests/test_pivot.py` com 5 testes (cabecalho exato + 4x7, regioes em ordem alfabetica, formato ponto/2 casas sem R$, total 931274.06 +-0.5, Sudeste 265077.49 +-0.5 e lider).
- Arquivos: pipeline/build.py, tests/test_pivot.py, pivot_receita.csv, scripts/ralph/prd.json, scripts/ralph/progress.txt
- Verificacao: `python -m py_compile pipeline/build.py tests/test_pivot.py` OK; `python -m pytest -q` = 9 passed; `git status --porcelain vendas.csv lojas.csv` vazio.
- **Learnings for future iterations:**
  - `pivot.columns.name = None` e necessario: sem isso o `to_csv` escreve uma linha extra com o nome do eixo (`mes`) antes dos dados e o cabecalho nao fica exato.
  - Gravar o pivot com `index=True` (a coluna de indice vira `regiao`, que e o primeiro campo do cabecalho exigido).
  - Valores do pivot (para US-003 embutir no HTML): Sudeste lidera com 265077.49; total 931274.06; meses 2026-01..2026-06 sem lacunas (fill_value=0.0 e so seguranca).
  - Para US-003, ler pivot_receita.csv com `pd.read_csv(..., index_col="regiao")`; colunas ja vem em ordem cronologica e servem de labels do eixo X.
---
